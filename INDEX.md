# End-to-end coverage ledger

Generated from `index.json` by dsh-migrate. Rows are written by the agent from the plugin's
documented features; `lastPassedFor` records the newest harness tag this feature was verified against.

Framework: playwright
Generated for: dsh dsh-v0.1.7-alpha.1, plugin @royenheart/dsh-plugin-mcp-support

| Feature | Surface | Entry | Expected | Checks | State | Last passed |
|---|---|---|---|---|---|---|
| `web-status-tab` | web-client | Session header view-tab row in the dsh web UI (right of the trajectory tab); click the tab named "mcp" | The mcp tab is visible and selected (aria-selected=true) and its status page (heading "MCP 状态" plus a "刷新" button) is rendered | aria, text, console | passing | dsh-v0.1.7-alpha.1 |
| `web-status-empty-state` | web-client | mcp view tab with no effective servers | The view shows "No MCP servers configured." and no server rows | aria, text, geometry:overflow, console | passing | dsh-v0.1.7-alpha.1 |
| `web-status-refresh` | web-client | "刷新" button in the mcp status page header | Exactly one new status request is committed and the view settles back to the same content | aria, text, http, console | passing | dsh-v0.1.7-alpha.1 |
| `status-endpoint-get` | web-client | GET /plugins/@royenheart/dsh-plugin-mcp-support/status as read by the browser view | 200 with {"ok":true,"servers":[...]}, one row per effective server; HEAD returns 200 with an empty body | http | passing | dsh-v0.1.7-alpha.1 |
| `status-endpoint-method-guard` | web-client | Non-GET/HEAD request to the status route | 405 with {"ok":false,"error":"method not allowed"} | http | passing | dsh-v0.1.7-alpha.1 |
| `status-endpoint-unknown-path` | web-client | Sibling path under the plugin's route prefix | 404, proving the plugin's route registration is exact rather than a prefix catch-all | http | passing | dsh-v0.1.7-alpha.1 |
| `composition-servers` | web-client | Bundle patch layer that declares mcp-support config.servers (a deployment defaults bundle) | Both composition servers appear in the effective list, in composition order, and mount | http, aria, text, console | passing | dsh-v0.1.7-alpha.1 |
| `settings-layer-override` | web-client | Profile cordis.patch.yml mcp-support override (what the dsh settings forms write) | The settings entry replaces the composition entry by serverName; the row reports the settings transport and stays first in order | http, aria, text, console | passing | dsh-v0.1.7-alpha.1 |
| `settings-layer-append` | web-client | Profile cordis.patch.yml mcp-support override with a settings-only server | The settings-only server is appended after every composition server and mounts | http, aria, text, console | passing | dsh-v0.1.7-alpha.1 |
| `js-expression-config` | web-client | !!js expressions in the composition and settings layers | Both servers mount, proving the Loader evaluated each layer's expression before the server schema normalized it (an unevaluated expression would fail activation) | http, aria, text, console | passing | dsh-v0.1.7-alpha.1 |
| `stdio-server-mount` | web-client | stdio server entry (command/args/env/cwd) | The row reports transport stdio and mounted: true, which requires the native child to have connected and synchronized tools | http, aria, visual | passing | dsh-v0.1.7-alpha.1 |
| `streamable-http-server` | web-client | streamable-http server entry (url/headers) | The row reports transport streamable-http and is listed in effective order (the native child mounts and retries in the background) | http, aria, text | passing | dsh-v0.1.7-alpha.1 |
| `status-view-server-rows` | web-client | mcp status page list of effective servers | One row per server in effective order, each showing the serverName text and the transport label | aria, text, geometry:containment, geometry:overlap, console | passing | dsh-v0.1.7-alpha.1 |
| `status-view-mounted-dot` | web-client | Mounted/not-mounted indicator dot on each status row | Dots are painted and visible with a real box; the mounted dot title is "mounted", the failed dot title is "not mounted", and their resolved background colours differ | visual, aria, geometry:containment | passing | dsh-v0.1.7-alpha.1 |
| `status-view-layout` | web-client | mcp status page layout inside the conversation view | Content is not clipped by the view box (no horizontal or vertical overflow), every row box is contained by the view, and consecutive rows do not overlap | geometry:overflow, geometry:containment, geometry:overlap | passing | dsh-v0.1.7-alpha.1 |
| `settings-import-legacy-yaml` | web-client | Legacy $DSH_HOME/settings.yaml mcp-support section, imported once by the harness settings service | settings.yaml is renamed settings.yaml.imported and its section lands in the profile patch (the persisted settings layer); its servers become effective | filesystem, http, aria, console | passing | dsh-v0.1.7-alpha.1 |
| `volatile-update-resync` | web-client | Committed volatile config update (settings import writes the profile override after boot) | The mounted set re-syncs without remounting the plugin: the imported server mounts, the route and the mcp tab stay live, and composition mounts survive | http, aria, text, console | passing | dsh-v0.1.7-alpha.1 |
| `committed-update-mount-failure` | web-client | Committed update whose server has failOnStartupError: true and a child that never connects | The route still answers 200, the dead row reports mounted: false with its mount error, and the previously mounted servers stay mounted | http, aria, text, visual, console | passing | dsh-v0.1.7-alpha.1 |
| `committed-update-duplicate-name` | web-client | Committed update whose settings list repeats a serverName | The wrapper rejects the update, leaves the previous mounts in place, the plugin entry stays active, and the status route reports 500 {ok:false,error:"duplicate serverName ..."} which the browser view renders as its error state | http, aria, text, console | passing | dsh-v0.1.7-alpha.1 |
| `headless-activation-no-webserver` | headless | dsh headless profile (dsh-base + dsh-headless) with the plugin installed; the profile mounts no web server | No "did not activate" or "pending (waiting for service: webServer)" diagnostic; the run reaches the credential/model boundary instead | process, console | passing | dsh-v0.1.7-alpha.1 |
| `native-mcp-tool-mount` | headless | Configured stdio MCP server in a headless profile | The fixture process writes its readiness marker only after the MCP protocol connection is established, proving the native child was spawned and connected | filesystem, process | passing | dsh-v0.1.7-alpha.1 |
| `fail-on-startup-error` | headless | stdio server entry with failOnStartupError: true whose child exits before connecting | Plugin activation is rejected: the loader prints "did not activate" for the mcp-support entry with the server name in the message | process | passing | dsh-v0.1.7-alpha.1 |
| `duplicate-name-activation-error` | headless | Two servers with the same serverName at activation time | Activation fails with the clear message "mcp-support: duplicate serverName \"dup\" — each MCP server must have a unique serverName" | process | passing | dsh-v0.1.7-alpha.1 |
| `invalid-server-name-rejected` | headless | A servers entry whose serverName violates the documented [A-Za-z0-9_-]{1,32} pattern | Activation fails at mount time with the loader's invalid-config diagnostic naming the entry (--dump-config deliberately does not validate) | process | passing | dsh-v0.1.7-alpha.1 |
| `install-py-install` | cli | python3 install.py install --profile web --home <home> | The package is symlinked into the profile node_modules, added as a link: dependency, and appended to dsh.profile.bundles; the profile's own cordis.patch.yml is untouched | process, filesystem | passing | dsh-v0.1.7-alpha.1 |
| `install-py-idempotent` | cli | Second run of install.py install | Every step reports "already ..." and the profile package.json is byte-identical | process, filesystem | passing | dsh-v0.1.7-alpha.1 |
| `install-py-uninstall` | cli | python3 install.py uninstall --profile web --home <home> | The symlink, link: dependency, and bundle entry are removed, a later --dump-config contains no plugin row, and the second uninstall exits cleanly | process, filesystem | passing | dsh-v0.1.7-alpha.1 |
| `dump-config-composition` | cli | dsh --profile web --dump-config | With the plugin installed the tree contains "- id: mcp-support" with name '@royenheart/dsh-plugin-mcp-support'; after uninstall it contains no plugin row | process | passing | dsh-v0.1.7-alpha.1 |
| `bundle-patch-row` | cli | Package-declared bundle patch (dsh.bundle.patch -> cordis.patch.yml) reconciled through dsh.profile.bundles | The bundle's own patch inserts the plugin row without any profile patch edit, and the profile cordis.patch.yml stays byte-identical | process, filesystem | passing | dsh-v0.1.7-alpha.1 |
| `settings-form-page` | web-client | dsh Plugins settings page form for the mcp-support entry's volatile servers field | No user-reachable form control exists for servers in this package; the layered sources are covered through the profile patch and the legacy settings.yaml import instead | aria | unknown | - |
| `manual-plugin-add` | cli | dsh plugin --profile <name> add link:<path> (the README's manual alternative to install.py) | Left unknown; install.py performs the same symlink + dependency + bundle reconciliation and is covered end to end | process | unknown | - |
| `js-expression-env-headers` | web-client | !!js expressions inside a server's env or headers mapping | Left unknown; evaluation of !!js values is proven for command and url in both layers by the layering spec | http | unknown | - |
| `tool-call-timeout-and-instruction-limit` | headless | Pass-through fields toolCallTimeoutMs and maxInstructionBytes on a server entry | Left unknown; the wrapper forwards these fields verbatim to the native bridge and adds no logic of its own | process | unknown | - |
| `reconnect-policy` | headless | Per-server reconnect policy passed through to the native mcp-client bridge | Left unknown; the package documents that it re-implements no reconnect logic, and unsupported field shapes are already rejected by the Config schema | process | unknown | - |
