# End-to-end coverage ledger

Generated from `index.json` by dsh-migrate. Rows are written by the agent from the plugin's
documented features; `lastPassedFor` records the newest harness tag this feature was verified against.

Framework: playwright
Generated for: dsh dsh-v0.1.6-alpha.1, plugin @royenheart/dsh-plugin-mcp-support

| Feature | Surface | Entry | Expected | Checks | State | Last passed |
|---|---|---|---|---|---|---|
| `status-tab` | web-client | Session header view-tab row in the shipped `web` profile; the tab labelled `mcp` that the plugin's browser half registers | An `mcp` tab exists immediately to the right of the Trajectory tab, in one non-overlapping row inside the viewport; selecting it renders the status page heading | aria, text, geometry:order, geometry:overflow, console | passing | dsh-v0.1.6-alpha.1 |
| `status-empty-state` | web-client | The `mcp` tab with no effective servers configured | The page shows the heading `MCP 状态`, the `刷新` control, and the exact text `No MCP servers configured.` with no rows and no overflow of its own box | aria, text, geometry:overflow, console | passing | dsh-v0.1.6-alpha.1 |
| `status-refresh` | web-client | The `刷新` button in the MCP status page header | A new GET to the status route is issued and the view settles on the same state without errors | aria, text, http, console | passing | dsh-v0.1.6-alpha.1 |
| `composition-server-list` | web-client | The plugin entry's `servers` list (profile patch row `mcp-support`), documented in README "Composition config example" | All three render in configured order with the right transport, mounted state, no overlap, no clipped names, and the route payload matches the visible list | aria, text, geometry:order, geometry:overflow, http, console | passing | dsh-v0.1.6-alpha.1 |
| `stdio-mount` | web-client | A `transport: stdio` server entry (composition or settings) | The fixture process starts and receives `initialize`, `notifications/initialized` and `tools/list`, proving the bridge spawned it and discovered tools | fs-marker, text, console | passing | dsh-v0.1.6-alpha.1 |
| `streamable-http-mount` | web-client | A `transport: streamable-http` server entry with `url` and `headers` | The HTTP fixture receives `initialize` and `tools/list` over HTTP and the row renders as mounted with transport `streamable-http` | fs-marker, text, http, console | passing | dsh-v0.1.6-alpha.1 |
| `mounted-state` | web-client | The per-row mounted indicator on the MCP status page | Each row reports `mounted` or `not mounted` consistently with the status route's `mounted` field | aria, text, http, console | passing | dsh-v0.1.6-alpha.1 |
| `settings-namespace-merge` | web-client | The persisted `mcp-support` settings namespace in `$DSH_HOME/settings.yaml`, documented in README "Settings example" | `shared` keeps its composition position but carries the settings transport, `gamma` is appended last, and the overridden composition server is never spawned | text, geometry:order, http, fs-marker, console | passing | dsh-v0.1.6-alpha.1 |
| `live-settings-resync` | web-client | Editing `$DSH_HOME/settings.yaml` while dsh is running (the `applies: 'live'` settings registration) | The added server mounts a real child process and appears in the list; after removal its row disappears and the host payload contains only the composition server | text, http, fs-marker, aria, console | passing | dsh-v0.1.6-alpha.1 |
| `mount-error-row` | web-client | A server whose mount rejects (only possible with `failOnStartupError: true`) | The failed row reads `not mounted` and carries the native bridge's error message, the healthy row stays mounted, the text wraps inside its row, and a refresh reproduces the same state | text, http, geometry:overflow, console | passing | dsh-v0.1.6-alpha.1 |
| `fail-on-startup-error` | headless | The `failOnStartupError` field of a server entry (documented default `false`) | With `false` the plugin row still activates and the healthy sibling mounts; with `true` the mount rejects and is reported as a mount error instead of silently retrying | cli-output, cli-exit, fs-marker, text | passing | dsh-v0.1.6-alpha.1 |
| `status-route` | web-client | `GET /plugins/@royenheart/dsh-plugin-mcp-support/status`, the session-agnostic endpoint the browser half reads | GET answers `200 {"ok":true,"servers":[…]}` with exactly `serverName`/`transport`/`mounted`, HEAD answers with no body, other methods answer `405 {"ok":false,"error":"method not allowed"}`, and the route matches exactly (sub-path 404) | http, console | passing | dsh-v0.1.6-alpha.1 |
| `headless-activation` | headless | `dsh --profile headless <task>` with the plugin installed; the headless profile mounts no `webServer` service | The plugin row activates without waiting for `webServer` (no pending/did-not-activate warning), mounts the fixture, and the run reaches the model call instead of stalling | cli-output, cli-exit, fs-marker | passing | dsh-v0.1.6-alpha.1 |
| `config-union-stdio` | cli | The `stdio` half of the native `@deepseek-ai/dsh-mcp-client` config union (`command`, `args`, `env`, `cwd`, `toolCallTimeoutMs`, `failOnStartupError`, `maxInstructionBytes`, `reconnect`) | The config is accepted, the fixture receives the `env` marker and runs from the configured `cwd`, and tool discovery completes | cli-output, cli-exit, fs-marker | passing | dsh-v0.1.6-alpha.1 |
| `config-union-streamable-http` | cli | The `streamable-http` half of the native config union (`url`, `headers`, `toolCallTimeoutMs`, `failOnStartupError`, `maxInstructionBytes`, `reconnect`) | Both forms are accepted; the composition entry completes a real HTTP handshake and the settings entry renders with transport `streamable-http` | cli-output, http, text, fs-marker | passing | dsh-v0.1.6-alpha.1 |
| `config-validation` | cli | Invalid `servers` entries in the composition config | The boot fails with the schemastery union error for the invalid name and with `duplicate serverName "…"` for the duplicate; neither is accepted silently | cli-output, cli-exit | passing | dsh-v0.1.6-alpha.1 |
| `settings-validation` | headless | A persisted `mcp-support` settings section that repeats a `serverName` | The section is refused at the settings seam with the same duplicate-name message, so a list that can never reconcile is not stored or mounted | cli-output, cli-exit | passing | dsh-v0.1.6-alpha.1 |
| `bundle-wiring` | cli | The package's `dsh.bundle.patch` (`cordis.patch.yml`) declaring the `mcp-support` host row | The composed tree contains the `mcp-support` row pointing at `@royenheart/dsh-plugin-mcp-support` and carries the composition `servers` config from the profile patch | cli-output, cli-exit | passing | dsh-v0.1.6-alpha.1 |
| `install-script` | cli | `python3 install.py install|uninstall --profile <name> --home <dsh home>`, documented in README "Install into a profile" | Both commands are idempotent and report what they did, the link/dependency/bundle entries appear and disappear together, the profile's own `cordis.patch.yml` is never modified, and the installed profile serves the `mcp` tab with the empty state | cli-output, cli-exit, aria, text, console | passing | dsh-v0.1.6-alpha.1 |
| `dev-surface` | cli | `npm run typecheck`, `npm run build`, `npm test`, documented in README "Develop" | All three exit 0; the build produces `lib/index.js` (host) and `lib/client.js` (browser) and the node:test suite reports zero failures | cli-output, cli-exit | passing | dsh-v0.1.6-alpha.1 |
